const Patterns = {
    fullNamePattern: {
        value: /([a-zA-Z]+\s){1,}([a-zA-Z]+)/
    },
    emailPattern: {
        value: /[a-z0-9]+@[a-z]+.[a-z]{2,3}/
    },
    onlyNumbersPattern: {
        value: /^[0-9\b]+$/
    }
}